"""
Companion 浅层意图策略 — 默认/quick 模式

Phase 7: 长策略段落改为 Scene Hints（一句话提示）。意图分类仍用于路由，
但 System Prompt 注入从 ~150 tokens/条 压缩到 ~50 chars/条。

保留 COMPANION_INTENT_PROMPTS 别名向后兼容。

==== 与 deep 模式的区别 ====

+------------------+---------------------------+---------------------------+
| 维度             | Companion（本文件）        | Deep（deep_strategies）    |
+------------------+---------------------------+---------------------------+
| 工具约束         | 无                        | TOOL_DEPENDENCY_CONSTRAINT |
| 早停强调         | 每类都有                   | 仅 lookup/realtime 有       |
| Scene Hint 长度  | ~50 chars                 | ~100 chars（多工具链提示）  |
+------------------+---------------------------+---------------------------+
"""

from __future__ import annotations

# ═══════════════════════════════════════════════════════════════════════════
# Scene Hints — 一句话场景提示（Phase 7）
# ═══════════════════════════════════════════════════════════════════════════

COMPANION_SCENE_HINTS: dict[str, str] = {
    "chitchat": "[场景：闲聊。直接回应，不要调工具——除非用户消息中混入了明确的数据查询。]",
    "factual": "[场景：常识问答。基于你的知识直接回答。不确定时优先搜索而非猜测。]",
    "lookup": (
        "[场景：查数据。search 返回的信息够用就直接答，不要自动调 detail。"
        "两次搜索无果就诚实告知。名称有歧义就追问。]"
    ),
    "discovery": (
        "[场景：推荐。3-5 部够用——你不是在做年度榜单。"
        "每部一句话理由。优先推荐评分高且最匹配的。]"
    ),
    "realtime": (
        "[场景：时效数据。并行调一次够用——拿到结果后不要逐个展开每个条目。]"
    ),
    "debate": (
        "[场景：争论。亮出你的立场，用数据佐证。"
        "观点和数据都要有——有论据的毒舌比空口争论有力。]"
    ),
    "emotional": (
        "[场景：陪伴。先共情回应情绪，再推荐。工具是附属品——"
        "情感连接优先于数据完整性。]"
    ),
    "unknown": "",
}

# ═══════════════════════════════════════════════════════════════════════════
# 向后兼容别名
# ═══════════════════════════════════════════════════════════════════════════

# Phase 7: 保留旧 key。调用方传入 intent_strategies=COMPANION_INTENT_PROMPTS
# 时，prompt_builder 仍能正确回退（走 deprecated path）。
COMPANION_INTENT_PROMPTS = COMPANION_SCENE_HINTS
