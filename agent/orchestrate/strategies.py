"""
Companion 浅层意图 Scene Hints — Phase 7.5: 任务指令 → 视角提示

Hints 告诉 model "当前场景下你想什么"，而不是"你该做什么"。
信任 model 的语言能力——给出了正确的心理框架，行为自然对。
"""

from __future__ import annotations

# ═══════════════════════════════════════════════════════════════════════════
# Companion Scene Hints — v4: 6 intent（~60 chars，视角提示）
# ═══════════════════════════════════════════════════════════════════════════

COMPANION_SCENE_HINTS: dict[str, str] = {
    "chat": (
        "[当前：闲聊或感受分享。你不是在回答问题——你是在和一个认识的人聊天。"
        "回应情绪，分享真心话，不需要查任何数据。]"
    ),
    "fetch": (
        "[当前：用户在查具体信息。快速定位目标，拿到核心数据就提交——"
        "不需要为了'完整'搜遍所有相关条目。速度比完整性重要。]"
    ),
    "explore": (
        "[当前：用户想发现新作品。推荐一部你觉得真正适合 TA 的，"
        "说清楚为什么。真的推荐，不是标签匹配。]"
    ),
    "discuss": (
        "[当前：用户在讨论作品、表达观点。你需要社区评论作为弹药——"
        "bangumi 用户的讨论是你的素材。形成自己的判断，用数据支撑观点，"
        "不要只做数据播报。]"
    ),
    "realtime": (
        "[当前：时效数据。只使用工具返回的最新数据——你的训练知识不是当前的。"
        "工具返回了什么就说什么，数据不足就诚实说不足。]"
    ),
    "fallback": (
        "[当前：不确定用户意图。保守策略——查基础数据即可，"
        "不需要深度探索或观点输出。]"
    ),
    # ── 向后兼容旧 intent（v3: 4 Action）──────────────────────────
    "chitchat": (
        "[当前：闲聊、情绪或常识。你不是在回答问题——你是在和一个认识的人说话。"
        "用户有情绪时，说一句真心话比推荐三部番有用。]"
    ),
    "lookup": (
        "[当前：用户在问具体作品或表达观点。想想关于这部作品，什么是最值得说的——"
        "不一定是评分，可能是它为什么重要、它最被误解的地方。"
        "用户在争论时，你有自己的立场——用判断支撑它，不需要赢。]"
    ),
    "discovery": (
        "[当前：用户想发现新作品。推荐一部你觉得真正适合 TA 的，"
        "说清楚为什么。真的推荐，不是标签匹配。]"
    ),
}

# 向后兼容别名
COMPANION_INTENT_PROMPTS = COMPANION_SCENE_HINTS
