# eval 数据来源与可信度

> 这份文档只回答一个问题：**报告上的每个数字，是从哪来的？可不可信？**
> "怎么跑"见 [`README.md`](./README.md)；这份只管"数据出处"。
>
> 建立于 2026-09-12，起因：RAG 评测报告里出现大片 `P@5 = 1.000 / MRR = 1.000 / NDCG@5 = 1.000`，
> 追查后发现这些数字不是测出来的。**追查过程与证据全部记录在本文，不依赖任何人复述。**
>
> **修订 2026-09-12（同日晚）**：§7 的 A 类三项已实施并重跑，结果见 §9。B/C 类未动。
>
> **修订 2026-09-12（深夜）**：B 类（分组报告 + 通道分层）已实施，结果见 §10。
> C 类（human 组候选池）仍未动。§2 有一处概括被修正 —— 见 §10.1。
>
> **修订 2026-09-12（同夜，后）**：§10.3 ② 的"消融基准不是生产配置"已修，
> 基准改为生产默认值 —— 结论整个反过来，见 §11。**已发布的数字无需修正**（§11.4）。
>
> **修订 2026-09-14**：§14 记录**工具返回超出记忆层容量**这个生产缺陷（`search_local_bangumi`
> 中位 20,522 token vs 单条上限 2000），以及它如何连带改掉轴 3 的证据口径 ——
> **01:42 那份 n=30 冻样本因此作废**。§15 记录轴 3 Tier 2 忠实性判官的出数与 κ 校准，
> 含一次**我自己写出来的缓存键缺陷**（它一度让 κ 涨到假的高度，见 §15.3）。

---

## 0. 结论速查

**这张表现在就是报告里的分组。** `--evaluate` 不再输出单一均值，而是按 A/B/C 三组分别呈现，
所以"能不能用"在报告里是直接可见的，不必回来查本文。

| 报告里的组 | 数字 | 标准答案来源 | 检索来源 | 能用吗 |
|---|---|---|---|---|
| — | **轴 1 分类**（102 + 35 条） | 人工拟定的产品定义 | 生产分类器本体 | ✅ 真数字 |
| **B** | 轴 2 auto · exact（7 条） | 人工写死 entity id | 三条通道融合 | ✅ 真数字 |
| **A** | 轴 2 auto · 结构化（18 条） | SQL 查库 | **同一个 SQL** | ⚠️ **只能当回归断言**（跌了=通道坏了），不能当质量指标 |
| **C** | 轴 2 human（9 条，p07 被丢弃） | 人工判（候选池由检索器自己产生） | 三条通道融合 | ⚠️ 偏乐观，只能定性 |
| **D** | 轴 2 倒着出题（450 条） | **从实体反编查询 → 答案唯一** | 生产检索（同 §12） | ✅ **本轴唯一的质量数字来源**（§13）；层 A 全量 450 已跑 |
| — | **轴 3 Tier 1**（30 场景） | 冻结样本上的纯函数（无 GT） | 生成侧已冻结，判定侧不检索 | ✅ 判定是死文件上的纯函数，不随 LLM 抖动 |
| — | **轴 3 Tier 2 忠实性**（138 断言） | 判官的 rubric（**没有人工 GT**） | 判官读冻结证据 | ⚠️ **判官与被评模型同源**；κ 校准 n=39（§15） |
| — | ~~全部 `NDCG@K`~~ | — | — | ✅ **已修**（§9） |
| — | **全部 `Recall@K`**（\|GT\| ≫ K 时） | — | — | ⚠️ 结构性打不满（**未修**，分组只是暴露了它） |

**一句话**：报告里的数字不是"好"或"坏"，是**来源不同**。曾经不标来源地并排放在一张表里，
是这份评测最根本的问题 —— **现已按来源分组呈现**（§10）。

---

## 1. 核心难点：搜索的"标准答案"从哪来

分类器（轴 1）没有这个问题。"你好"该分到 `chat` —— 这是**产品定义**，写下来就是标准答案。

搜索不一样。问：「**关于音乐乐队的百合动画**」的正确答案是哪些作品？

- 库里有 1500 部作品
- 没有任何一列叫「是不是关于音乐乐队的百合动画」
- 谁也不知道答案 —— 除非把 1500 部挨个看一遍

**"标准答案"不能凭空产生。这是搜索评测全部复杂性的来源。**

于是只能走两条路，这就是"两个组"的由来 —— 不是术语，是被逼出来的分类：

| 组 | 判据 | 条数 |
|---|---|---|
| **auto** | 答案能从数据库某个字段直接查出来 | 25 |
| **human** | 答案不在任何字段里，只能人来判 | 10 |

---

## 2. auto 组：抄答案

### 2.1 设计意图

「芳文社」—— 每条作品都有 `tags` 字段，有些写着 `芳文社`。所以：

```sql
SELECT id FROM rag_entities WHERE tags @> 芳文社   → 31 条
```

31 条就是客观、完整、零成本的标准答案。**这个设计本身是对的。**

### 2.2 实际发生了什么

```
第一步  造标准答案 :  WHERE tags @> 芳文社                     → 31 条
第二步  拆条件     :  _parse_keyword_filters() 正则抠出「芳文社」
第三步  "检索"     :  WHERE tags @> 芳文社 ORDER BY popularity → 10 条
                      └────────── 与第一步同一个 WHERE ──────────┘
第四步  对比算分   :  P@5 = 5/5 = 1.000     ← 报告上的数字
                      MRR = 1.0            ← 报告上的数字
                      NDCG@5 = 1.0         ← 报告上的数字
```

**第一步和第三步是同一个条件。**「检索」就是「标准答案查询」加了个 `ORDER BY`。

用图书馆打比方：

> 题目：「说出 31 部芳文社的作品」
> **正常**：你进馆自己找 10 部 → 跟标准答案一对 → 找到 5 部 → 得分 5/31
> **现在**：老师拿答案单进馆抄了 10 部 → 再跟答案单一对 → **全对！满分！**

### 2.3 为什么不只是"数字虚高"，而是"零信息量"

19 条候选里 18 条走这条路（tag 12 + year 2 + score 1 + career 3）。

关键在于 `_run_retrieval()` 的融合顺序是 **keyword 优先**：

```python
merged = list(keyword_ids)        # rag_eval.py:411 —— keyword 先占满
for nid in name_ids: ...          # 再补 name
for vid in vector_ids: ...        # 最后补 vector
return merged[:limit]             # limit=10
```

`_run_keyword_search(limit=10)` 实测返回**恰好 10 条**。所以 `merged[:10]` 完全由 keyword 通道决定 ——

> **向量通道在这 18 条查询上贡献 0 条。**
> **即使向量检索彻底坏掉，这 18 条的数字一个都不会变。**

这不是"测了个没价值的东西"，是"测了个永不会失败的断言"。而它**同时掩盖**了真正想知道的事：语义检索能不能理解「芳文社」这种专有名词。

### 2.4 证据（可复现）

```bash
.venv/bin/python -c "
from eval.rag_queries import AUTO_QUERY_DEFS
from eval.rag_eval import _parse_keyword_filters, _run_keyword_search
qid,qtext,et,st,cat,sql = AUTO_QUERY_DEFS[0]
f = _parse_keyword_filters(sql)
print('抠出的条件:', f)
ret = _run_keyword_search(entity_type=et, subject_type=st, limit=10, **f)
print('检索返回:', len(ret), '条')
"
# → 抠出的条件: {'required_tags': ['芳文社']}
# → 检索返回: 10 条   （= limit，占满全部名额）
```

### 2.5 附带缺陷：GT 含 nsfw，检索器被禁止返回

| | WHERE 子句 |
|---|---|
| GT 生成 SQL | `entity_type='subject' AND subject_type=2` |
| `_run_keyword_search:140` | 上述 **+ `nsfw == False`** |

实测 **a18「动画制作人」的 58 条 GT 里有 10 条是 `nsfw = true`** —— 检索器被明令禁止返回它们，却被算进 Recall 的分母。

> **已修（§9-A2）**：GT 的 SQL 现在追加 `AND nsfw = false`，与检索范围对齐。a18 的 GT 由 58 → 48。

---

## 3. human 组：自己出考卷

### 3.1 设计意图

「关于音乐乐队的百合动画」—— 库里没这个字段。只能把候选摆出来，**人一个个判**。

### 3.2 候选池是被测系统自己产生的

```python
# rag_eval.py:223  build_pooled_template()
results = retriever.hybrid_search(query=qtext, ..., limit=15)   # ← 被测的检索器
```

> 想知道你找东西行不行。
> 方法是：**让你自己**先写下你能想到的 20 样东西。
> 然后检查：这 20 样里有多少是真正需要的？

**你想不到的，根本没进这张纸**，也就永远不会被发现"漏了"。

这是标准的**池化偏差**（pooling bias）。经典 TREC 的做法是**多个不同系统各跑一遍、取并集**当候选池 —— 一个系统漏的，别的系统还能捞出来。这里只有一个系统，所以：

> 凡是检索器捞不出来的，自动从考卷上消失 → 分数天生偏高。

### 3.3 另一条：GT 大小被池子大小封顶

human 组 GT 为 4–16 条，池子约 20 个候选。GT 不是"库里真正相关的有多少"，而是"这 20 个候选里有几个被判为相关"。

### 3.4 p07 被静默丢弃

`p07「傲娇双马尾美少女」`：20 个候选**全部标 false** → GT 为空 → `cmd_evaluate:498` 的 `if not gt: continue` 跳过它。

**35 条查询 → 34 条结果，少的那条没有任何提示。**

而 p07 恰恰是最有价值的一条 —— 它说明检索对这条查询完全跑偏。

> **已修（§9-A3）**：`_split_by_gt()` 分离空 GT 的查询，报告末尾单列"未参与统计的查询"，
> 并写入 `ground_truth_merged.json` 的 `dropped_queries` 字段。`--ablate` 同一处也改了。

---

## 4. NDCG 算错了 —— 已修（§9-A1）

### 4.1 `eval/metrics.py` 是一座孤岛

仓库里有**两份**指标实现：

| 文件 | 实现 | 消费者 |
|---|---|---|
| `eval/metrics.py` | `recall_at_k` / `precision_at_k` / `mrr` / **`ndcg_at_k`** | **0 处** |
| `eval/rag_eval.py:421-467` | `_dcg` / `_ndcg` / `_compute_metrics` | RAG 评测全部 |

实测：`metrics.py` 的四个 IR 指标在整个仓库（除自身）**引用数为 0**。

**而 `metrics.py:115-119` 的 `ndcg_at_k` 写得完全正确**：

```python
ideal_order = sorted(ground_truth.values(), reverse=True)   # ✅ 全集 GT 降序
```

**正确的版本一直躺在隔壁文件里。`rag_eval.py` 自己重写了一份错的。**

### 4.2 bug ①：IDCG 参照系错误

```python
# rag_eval.py:425-429
def _ndcg(relevances):
    dcg = _dcg(relevances)
    ideal = sorted(relevances, reverse=True)   # ❌ 只排了「本次返回的 k 条」
    idcg = _dcg(ideal)
    return dcg / idcg if idcg > 0 else 1.0
```

解剖 p01（GT = 6 条，命中在第 3、4 位）：

```
DCG  = 1/log2(4) + 1/log2(5)                     = 0.9307

IDCG = _dcg(sorted([0,0,1,1,0], reverse=True))   = 1.6309   ← 只排了本次返回的 5 条
       报告值 = 0.9307 / 1.6309 = 0.571  ✓ 与报告对得上

IDCG = _dcg([1,1,1,1,1])                         = 2.9485   ← 应为全集 GT 的理想排序
       正确值 = 0.9307 / 2.9485 = 0.316

       → 虚高 1.81 倍
```

标准定义（Järvelin & Kekäläinen, 2002）：IDCG 是**集合中所有相关文档**的理想排序，不是检索结果自身的降序重排。

### 4.3 bug ②：检索彻底失败给满分

```python
return dcg / idcg if idcg > 0 else 1.0   # ← 全零时返回 1.0
```

```
_ndcg([0,0,0,0,0])  → DCG=0, IDCG=0 → 返回 1.0   ← 一条没召回 = 满分
_ndcg([])           → 同上        → 返回 1.0   ← 零结果 = 满分
```

现有数据里尚未触发（所有查询都有命中），但是活的地雷。

> **已修（§9-A1）**：私有 `_dcg`/`_ndcg` 已删除，改用 `eval.metrics.ndcg_at_k`
> （IDCG 取自全集 GT，零命中返回 0.0）。`rag_eval.py` 的 `import math` 随之成为死导入，一并删除。

---

## 5. Recall@K 的结构性天花板

| query | GT | 返回 | R@5 理论上限 | 报告值 |
|---|---|---|---|---|
| a03 京都动画 | 49 | 10 | 10/49 = 0.204 | 0.102 |
| a18 动画制作人 | 58 | 10 | 10/58 = 0.172 | 0.086 |
| a16 声优 | 55 | 10 | 10/55 = 0.182 | 0.091 |

auto 组 GT 中位数 **24**（最大 58），检索只返回 **10** 条。

> `Recall@5` 在 |GT| ≫ K 时不是"检索做得差"的证据 —— 分母里塞了 40+ 条永远进不了 top-5 的文档。

`rag_eval.py:27` 的注释写"auto（自动 GT，Recall 精确）"。**GT 确实是精确完整的 —— 而"完整"恰恰是让 Recall 不可解释的原因。**

---

## 6. 三种检索通道（背景知识）

库 = 一张表，1500 行。每行一部作品：`id / name / tags / year / score / 简介`。

检索 = 从 1500 行里找相关的。三条路：

| 通道 | 实现 | 优点 | 缺点 |
|---|---|---|---|
| **① 结构化** | SQL `WHERE tags @> 芳文社` | 准 | **死板** —— 只能字面匹配，用户说不出 tags 里的原词就查不到 |
| **② 名称** | trigram `name_search()` | 快 | 只认名字 |
| **③ 向量语义** | pgvector `hybrid_search()` | 灵活，能理解意思 | 模糊，可能召不相干的 |

**通道 ③ 需要"索引"**：把每部作品的简介喂给 embedding 模型 → 一串 1024 维数字；意思相近的作品数字串也相近。1500 × 1024 挨个比太慢，**索引是提前建好的加速结构**；pgvector 是 PostgreSQL 的插件，专干这个。

**生产环境三条通道融合使用**（`_run_retrieval`，顺序 keyword → name → vector）。

**而这正是评测的盲区**：
- auto 组用 SQL 造答案，又用 SQL 检索 → **只测了通道 ①，通道 ③ 完全没被覆盖**
- human 组三条都跑了，但答案池是系统自己出的（见 §3）

---

## 7. 修法

### A 类 · 明确的 bug，无设计争议 —— ✅ 2026-09-12 已实施，见 §9

| # | 改什么 | 位置 | 状态 |
|---|---|---|---|
| A1 | 删私有 `_dcg`/`_ndcg`，改用 `eval.metrics.ndcg_at_k` | `rag_eval.py` | ✅ |
| A2 | GT SQL 加 `nsfw = false`，与检索对齐 | `rag_eval.py` `build_auto_gt()` | ✅ |
| A3 | GT 为空不再静默 `continue`，报告丢弃条数与原因 | `rag_eval.py` `_split_by_gt()` | ✅ |
| A4 | `--build` 防呆：已有标注时拒绝重建池化模板 | `rag_eval.py` `cmd_build()` | ✅ 追加 |

> 注：`metrics.ndcg_at_k` 接受 `dict[str, int]`（分级相关度），当前是二值 —— 传 `{id: 1 for id in gt}` 即可。
> 改完 A1 后，`_compute_metrics` 里 `NDCG@K` 的语义变为"前 K 位命中数"的折损加权，与 `Precision@K` 单调相关。信息量仍有限，但至少正确。

#### A4 的由来（原计划外）

A2 要求重跑 `--build`，但 `cmd_build()` 的 Phase 2 会**重建池化模板并把 `relevant` 全部重置为 null**
—— 即销毁已有的 183 条人工标注。这是执行 A2 时才会暴露的数据安全问题，已加防呆：
检测到已有标注则跳过重建并提示"确实要重建请先删除该文件"。

### B 类 · 报告形状 —— ✅ 2026-09-12 已实施，见 §10

| # | 改什么 | 状态 |
|---|---|---|
| B1 | auto 组 18 条 → 独立成「A 自测组」，与 B/C 组分开呈现 | ✅ |
| B2 | 新增**通道分层**：keyword / name / vector 各自单独检索的指标 | ✅ |

**B1 — auto 组改「按通道分层报告」而不是删除。**（建议）

不要问"融合后 P@5 多少"，改问"**每条通道各自找到几部**"：

```
a01 芳文社  (GT=31)
  keyword 通道 : 10/10 命中   P@5=1.0    ← 永远为真，降级为回归监控
  name    通道 :  0/10 命中               ← 有信息量
  vector  通道 :  ?/10 命中               ← 有信息量 ★ 这才是想知道的
  融合后       : P@5=1.0（全部来自 keyword，vector 贡献 0）
```

**为什么建议改造而非删除**：SQL 造标准答案本身是对的（客观、完整、零成本），错的只是"用同一个条件去检索"。分层后，这 18 条从废数据变成**语义通道的探针** —— 它回答"向量模型认不认得「芳文社」「ufotable」这类专有名词"。大概率答得很差，**但差本身是有价值的架构结论**（说明这类查询必须靠 keyword 兜底）。

**B2 — `Recall@K` 在 |GT| > K 时标注不可解释**，或对 auto 组改用 `Precision@K` 作主指标。

### C 类 · 需要重新标注（人工成本）—— ❌ 2026-09-12 决定【不做】

**C1 — human 组候选池重造**：用**多个不同配置的并集**替代单一配置。

现在 `build_pooled_template` 只用 `hybrid_search(limit=15) × 2 路`。仓库里已有现成的 5 个消融配置（`ABLATION_CONFIGS`）+ `name_search` + `_run_keyword_search` —— 全部跑一遍取并集，能把"任一通道能找到的"都纳入池子。

**代价**：候选数会从 ~20 涨到 ~40–50，需要重新人工标注（183 → 400+ 条）。

**已决定不做**（2026-09-12）。理由：投入产出不成比例 —— 重标 400+ 条换来的是 9 条查询的**定性**数字，而池化偏差**不会因此消失**（新池子同样由被测系统产出，只是更宽）。C 组继续只作定性使用，偏差写在报告头。

### D 类 · 需要拍板的决定

1. ~~**auto 组 18 条**：改造还是删除？~~ → ✅ 已改造（分层报告，见 §10）
2. ~~**human 组候选池**：重造还是接受偏差？~~ → ❌ 已决定不重造（见上）
3. **`Recall@K` 的去留**：保留但标注局限，还是换 `Precision@K` 当主指标？**仍未决**
4. **GT 封顶（即"改 GT 定义"）**：把标准答案从"全部匹配条目"改为"用户会接受的前 N 个"。
   ❌ **已决定不做**（2026-09-12）。它是唯一能真正消除 §5 天花板的办法，但代价是引入
   一个新的主观决定（"前 N 个"按什么排？）并重生成全部 GT。理由同 C 类：投入产出不成比例 ——
   而且检索下游还有一层 LLM 在挑，`Recall@5` 打不满**不直接等于用户不满**。
   **A 组继续只作回归断言用**：它跌了 = keyword 通道坏了。

---

## 8. 这份文档本身的可信度

- 所有代码位置（文件:行号）于 2026-09-12 逐一核对
- 所有数字（GT 大小、命中数、NSFW 条数、NDCG 算术）来自对本地库（1500 条实体）与 `eval/data/rag_gt/` 的实际执行，非推算
- §2.4、§4.2 的复现命令附在正文中，可独立验证
- §1–§6 描述的是**修改前**的状态；每处修复在对应小节用引用块标注

---

## 9. A 类修复的实际效果（2026-09-12 重跑）

### 9.1 修改清单

| # | 文件 | 改动 |
|---|---|---|
| A1 | `eval/rag_eval.py` | 删 `_dcg`/`_ndcg` + `import math`；改 `from eval.metrics import ndcg_at_k`；`_compute_metrics` 的 NDCG 段改调 `ndcg_at_k(retrieved_ids, {id:1 for id in gt}, k)` |
| A2 | `eval/rag_eval.py` | `build_auto_gt()` 的 SQL 追加 `AND nsfw = false` |
| A3 | `eval/rag_eval.py` | 新增 `_split_by_gt()`；`cmd_evaluate` 与 `cmd_ablate` 均改用它；报告末尾新增"未参与统计的查询"；`ground_truth_merged.json` 新增 `dropped_queries` 字段 |
| A4 | `eval/rag_eval.py` | 新增 `_count_annotated_pooled()`；`cmd_build()` 检测到已有标注则跳过 Phase 2 |

### 9.2 改前 / 改后

```
指标                    改前        改后         变化
Recall@5          0.4167    0.4172    +0.0005
Precision@5       0.6941    0.6941    +0.0000
NDCG@5            0.9243    0.8582    -0.0661     ← 去掉虚高
MRR               0.9020    0.9020    +0.0000
```

逐条（只列有变化的 10 条）：

```
id    源       GT        NDCG@5              R@5
a18   auto   58→48   1.0000 → 1.0000    0.0862 → 0.1042   ← GT 分母修正
p01   human      6   0.5706 → 0.3156    0.3333 → 0.3333
p02   human     16   0.6183 → 0.4469    0.1875 → 0.1875
p03   human      7   0.8529 → 0.6164    0.4286 → 0.4286
p04   human      9   0.5438 → 0.3008    0.2222 → 0.2222
p05   human     10   0.5438 → 0.3008    0.2000 → 0.2000
p06   human      4   0.5000 → 0.1952    0.2500 → 0.2500
p08   human      4   0.9197 → 0.5856    0.5000 → 0.5000
p09   human      8   0.9558 → 0.8304    0.5000 → 0.5000
p10   human      4   0.9197 → 0.5856    0.5000 → 0.5000
```

**三个要点：**

1. **`p01` 的 0.3156 与手工推算逐位吻合**（§4.2）。其余 human 组的降幅同理 —— 那些 0.9x 的 NDCG 是 IDCG 参照系错误造成的虚高。
2. **a18 的 `Recall@5` 上升（0.0862 → 0.1042）不是改进，是修正** —— 分母去掉了 10 条永远拿不到的 nsfw 条目。引用时务必说清方向，否则会被误读成"检索变好了"。
3. **auto 组的 NDCG@5 仍是 1.0000，但这次是对的** —— 前 5 位全在 GT 里时，IDCG@5 就等于 DCG@5。**它正确，但依然没有信息量**（原因见 §2.3，不是 bug）。B 类未做之前，auto 组的这一列不要引用。

### 9.3 验证 A1 的三条断言

```python
ndcg_at_k([], gt, 5)                        # → 0.0   （改前 1.0：零结果得满分）
ndcg_at_k(['a','b','c','d','e'], gt, 5)     # → 0.0   （改前 1.0：全不相关得满分）
ndcg_at_k(['a','b','x0','x1','c'], gt6, 5)  # → 0.3156（改前 0.5706）
```

### 9.4 未做

- **C 类**（human 组候选池重造）—— 待拍板
- **GT 封顶**（§5 的结构性天花板）—— 分组只是把它**暴露**出来，没有消除
- `rag_eval.py` 仍未写**报告文件**（只打印 + 写 `ground_truth_merged.json`），
  不满足 README §方法论约定 3"报告头记 settings 快照"。本次改动的数字无独立报告存档。
- 重跑 `--build` 时**未重新生成池化模板**（A4 防呆按预期拦下），183 条标注完好；
  备份在 `docs/tmp/pooled_annotate.backup-20260912.json`（该目录 gitignore）

---

## 10. B 类修复的实际效果（2026-09-12）

改的是**报法**，不是检索。数字全部来自 `python -m eval.rag_eval --evaluate`。

### 10.1 分组指标

```
  组      #Q    Recall@5 Precision@5      NDCG@5         MRR
  A      18      0.2258      1.0000      1.0000      1.0000
  B       7      1.0000      0.2000      1.0000      1.0000
  C       9      0.3468      0.4667      0.4641      0.6296
  ─────────────────────────────────────────────────────────
  ALL    34      0.4172      0.6941      0.8582      0.9020
```

**ALL 行与 §9.2 的改后数字逐位相同** —— 重构是行为保持的，只改了呈现。

#### 修正一处 §2 的说法

§2 把 A 组概括为"恒等于 1.0"。**不准确**：`Recall@5` 实测 0.2258。

准确的说法是：

| 指标 | 值 | 为什么 |
|---|---|---|
| `Precision@5` / `NDCG@5` / `MRR` | **恒 1.0** | 返回的每一条都必然在 GT 里（同一个 WHERE） → 构造出来的 |
| `Recall@5` | 0.2258 | **不是 1.0**，但同样无信息量 —— 被 §5 的天花板压住（`\|GT\|` 9~55 ≫ K=5） |

四个数都没有信息量，**但理由是两套**。混为一谈会让人以为"只要 Recall 不等于 1 就还有救"。

### 10.2 通道分层 —— 本次最有价值的产出

```
  组      #Q   keyword      name    vector    merged
  A      18    0.2258    0.0000    0.0545    0.2258
  B       7    0.0000    1.0000    0.8571    1.0000
  C       9    0.0000    0.0000    0.3607    0.3468
```

**三条通道各管一类查询，几乎不重叠**：

| 通道 | 唯一能覆盖的组 | 机制 |
|---|---|---|
| `keyword` | A 组（标签/年份/评分/职业） | SQL 硬过滤，与 GT 同源 |
| `name` | B 组（精确名称） | trigram 名称匹配，7/7 全中 |
| `vector` | C 组（语义） | 1024d 余弦召回 |

**A 组的融合结果构成：180 条 = keyword 179 + vector 1 + name 0。**

→ 即 §2 说的"向量通道贡献 0"**首次被实测确认**。注意它**不是**"向量找不到"：
a04 `ufotable` 向量单独能找到 6/10，a16 `声优` 能找到 10/10 —— 是 **keyword 先把 10 个名额占满，
把它们挤出去了**。这两件事的修法完全不同（前者要换模型，后者只要改融合顺序）。

### 10.3 两个只有分层才看得见的问题

**① C 组融合后反而变差**

```
C 组：vector 单独 0.3607  >  merged 0.3468
```

name 通道在语义查询上返回了**不相关**结果（C 组 name recall = 0），
按 `keyword > name > vector` 的顺序，它们排在 vector 之前，**把相关结果挤出了前 10**。

只看融合数字，这个损失是不可见的 —— 反而会以为"融合作了正向工作"。

**② `--ablate` 的 baseline 不是生产配置 —— ✅ 已修，见 §11**

```
生产        tools/bgm_tools.py:1078  → 不传消融参数 → 用 hybrid_search 默认值
hybrid_search 默认                    enable_bucketing = False
--evaluate                           不传 → 默认 → 与生产【一致】✓
--ablate「baseline (全开)」            显式 enable_bucketing = True → 与生产【不一致】✗
```

所以消融表里那行标着"全开"的 baseline，**是生产从不使用的配置**；而真正等于生产的
`no_bucketing` 行，名字听起来像是"关掉了某个组件"。实测差异：

```
baseline (全开)  Recall@5 = 0.4103
no_bucketing    Recall@5 = 0.4172   ← 这才是 --evaluate 和生产的值
```

## 11. 消融基准改为生产配置（2026-09-12，已实施）

### 11.1 改了什么

新增 `PRODUCTION_RETRIEVAL` 常量作为**唯一事实来源**，`--evaluate` 与 `--ablate` 都从它派生：

```python
PRODUCTION_RETRIEVAL = {          # = rag/retriever.py:hybrid_search 的签名默认值
    "enable_threshold": True,
    "enable_bucketing": False,    # ← 生产默认【关】。旧的「全开」是虚构配置
    "enable_mmr": True,
}
PRODUCTION_DISTANCE_THRESHOLD = 0.65   # 旧值 0.9，与生产不一致
POOLING_DISTANCE_THRESHOLD = 0.9       # 池化【故意】不跟生产：池化要尽量多捞候选
```

`ABLATION_CONFIGS` 重写为「以生产为基准，每行只动一个开关」：

| 命名 | 含义 |
|---|---|
| `baseline (生产配置)` | 线上跑的那套 |
| `-xxx` | 把生产**开着**的关掉 |
| `+bucketing` | 把生产**关着**的打开 —— 这不是消融，是**候选改动** |

已核对：四个常量与 `RagEntityRetriever.hybrid_search` 的签名默认值**逐项一致**（脚本验证）。

### 11.2 改完之后：结论整个反过来了

```
  配置                             Recall@5    Precision@5            MRR         NDCG@5          Hit@5
  baseline (生产配置)                0.4172         0.6941         0.9020         0.8582         1.0000
  -threshold                  0.4172 +0.0000  0.6941 +0.0000  0.9020 +0.0000  0.8582 +0.0000  1.0000 +0.0000
  +bucketing                  0.4103 -0.0069  0.6824 -0.0118  0.8858 -0.0162  0.8449 -0.0132  1.0000 +0.0000
  -mmr                        0.4143 -0.0029  0.6882 -0.0059  0.8995 -0.0025  0.8536 -0.0046  1.0000 +0.0000
  vanilla (全关)              0.4143 -0.0029  0.6882 -0.0059  0.8995 -0.0025  0.8536 -0.0046  1.0000 +0.0000
```

旧表述"no_bucketing +0.0069"读起来像"分桶有害，该关" —— 而生产**本来就关着**，
那个"改进"是幻觉（它只是在跟一个虚构的基准比）。新表的读法：

| 行 | delta | 结论 |
|---|---|---|
| `-threshold` | **+0.0000**（逐位相同） | **`enable_threshold` 在这份数据上完全无效** —— 关掉零影响。要么删掉这个组件，要么它的价值不在检索指标上 |
| `+bucketing` | −0.0069 / −0.0118 / −0.0162 | **不该在生产开分桶。** 当前生产配置是对的 |
| `-mmr` | −0.0029 | MMR 有**微小正向**贡献，留着 |
| `vanilla (全关)` | −0.0029 | 与 `-mmr` 完全相同 —— 因为 threshold 关了没差别，再次印证第一行 |

> **`+bucketing` 行是这次重构最大的收获**：它把"要不要改生产"变成一个能直接读的表行。
> 结论是**不改**。

### 11.3 分组表暴露的稀释倍数

```
  配置                              A 自测        B 质量        C 人工
  baseline (生产配置)             0.2258      1.0000      0.3468
  -threshold                      0.2258      1.0000      0.3468
  +bucketing                      0.2258      1.0000      0.3206
  -mmr                            0.2258      1.0000      0.3357
  vanilla (全关)                  0.2258      1.0000      0.3357
```

`+bucketing` 对 **C 组**是 **−0.0262**，而全局表只显示 **−0.0069** ——
**被 A/B 组 25 条恒定不变的查询稀释了 3.8 倍**。

A 组与 B 组对所有配置**完全不变**，符合预期：这两个开关只作用于向量通道，
而 A/B 组分别由 keyword / name 通道决定。

### 11.4 `distance_threshold` 对齐后：`@5` 逐位未变，`@10` 有变化

把 eval 的 `distance_threshold` 从 0.9 改成生产的 0.65，**逐查询比对改前改后的 34 条**
（比对全部 16 个指标键）：

- **`@1` / `@3` / `@5` 的全部指标：34/34 逐位相同** —— 包括报告上那四个数
  （`0.4172 / 0.6941 / 0.8582 / 0.9020`）。所以 **`@5` 口径下已发布的数字不需要修正**。
- **`@10` 及返回条数有变**：`p05` 的 `Recall@10 / Precision@10 / NDCG@10` 变化
  （0.6 / 0.6 / 0.4800 → 0.5 / 0.5 / 0.4164）；`a23` 返回 10 → 5 条、`a25` 10 → 3 条。
  被砍掉的都是排在 6–10 位的低分向量结果，**够不到 `@5`**，所以 `@5` 指标纹丝不动。

> 注意早先只在 **C 组**上量过这个影响（10.0 → 9.9 条），"约等于 0"对 `@5` 成立，
> 但**对 B 组（exact）不成立** —— a23/a25 的返回条数直接腰斩。
> 报告只出 `@5`，所以没有对外影响，但**别把这个结论外推成"阈值随便调都行"**。

### 11.5 仍然没解决的

- **A 组的同源问题本身**：无法在报告层消除，只能标注。真要消除得改 GT 定义（取"前 10 个
  用户会接受的结果"而非"所有匹配条目"）
- **B 组 `Precision@5` = 0.2 是假象**：GT=1 而 K=5，命中即 1/5。这不是"精确率低"，
  是**指标不适用于 known-item 查询**。看 B 组只看 `Recall@5` / `MRR` / `NDCG@5`（都是 1.0，
  即 7/7 全中且都排在第 1 位）
- **C 组的池化偏差**（C 类，待拍板）
- **`enable_threshold` 零效果**这个发现本身没有下文 —— 它是死代码还是有用但指标测不出，
  未查

> ⚠️ **本节（§11）记录的是 2026-09-12 的状态。§12 之后，本节的两处结论已被推翻或改写：**
> `enable_threshold` 的"零效果"**已解释**（12.6），`-mmr` 的 −0.0029 **已归因**（12.6）。
> 表里的绝对数字（0.4172 等）是**影子实现口径**，已作废，见 12.2。

---

## 12. 轴 2 改跑生产路径（2026-09-13，已实施）

### 12.1 改了什么：评测口径从"影子实现"换回"线上那条路"

**改之前**，`rag_eval.py` 重写了生产检索的**两层**：

| 层 | 生产实际做的 | 旧 eval 自己做的 |
|---|---|---|
| 过滤器解析 | `tools/bgm_tools.py:_extract_keyword_filters` —— 从**查询文本**抽条件（规则 + 兜底） | 从 **GT 的 SQL** 反解出 WHERE，塞给检索器 |
| 融合 | `_unified_search` —— **短路**：keyword 凑满 limit 就不调 name/vector | 无条件跑三条通道再按 `keyword > name > vector` 去重截断 |

向量通道的参数本来就逐项一致，问题全在这两层。**后果**：评测量的不是线上行为，
而是一个从没在生产跑过的组合。

**改之后**：`_production_search` 走 `_extract_keyword_filters → _unified_search`，
与线上工具 `search_local_bangumi` 同一条路。

> **与线上的唯一有意差异是 `limit`**：线上由 LLM 在工具参数里定（默认 **5**），
> 评测固定 `RETRIEVAL_LIMIT = 10` 以支撑 `@1/@3/@5/@10` 网格。比较两边的数字时先想这一条。

**对拍验证**：`_production_search` 与线上工具 `search_local_bangumi.ainvoke`
在 4 条查询上返回的**名字序列完全一致**（见交接文档 §1 的实测记录）。

`_probe_search`（三通道各自单跑）**保留**，但从此明确标注为**非生产行为** ——
它唯一的用途是回答「该修哪条通道」，这个结论融合数字给不了（12.3）。

**复现**：`python -m eval.rag_eval --evaluate`（报告抬头会打印检索口径与 limit 差异）

### 12.2 数字对照：哪些作废、哪些沿用

影子实现口径 → 生产路径口径，**分组结构本身也变了**（A 组 18 → 15，多出一个 A′ 组）：

| 组 | 旧（影子实现） | 新（生产路径） | 说明 |
|---|---|---|---|
| A 自测 | 18 条 `R@5=0.2258` | **15 条 `R@5=0.2230`** | a13/a14/a15 移出 —— 生产解析**复现不了** GT 的 WHERE，它们不是自测组 |
| **A′ 解析缺口** | — | **3 条 `R@5=0.1063`** | 新组。量的就是"解析器达不到 GT"的那部分 |
| B 精确名称 | 7 条 `1.0000` | 7 条 `1.0000` | 不变（靠 name 通道，纯 SQL） |
| C 池化人工 | 9 条 `0.3468` | **9 条 `0.3068`** | ⚠️ 见下 |
| ALL | 34 条 `0.4172` | 34 条 `0.3949` | 仅供参考（含 A 组构造值） |

> ⚠️ **C 组的 0.3468 → 0.3068 是【两个原因】叠加的，别只归给一个：**
> ① **检索域从宽变窄** —— `subject_type` 回填修复（12.2.1）；
> ② **改走生产路径** —— 短路让一部分语义查询根本不跑向量通道。
> 两个方向都是往下压，量级相当。

**§9 / §10 / §11 里所有 `@5` 的绝对数字（`0.4172 / 0.6941 / 0.8582 / 0.9020` 等）
自本节起作废**，只在描述"当时改了什么"时有意义。

#### 12.2.1 `subject_type` 回填（与本次改动同批）

`_load_annotated_pooled()` 里 `pq.get("subject_type")` **恒为 None** ——
`pooled_annotate.json` 根本没有这个键（实际键只有
`candidates/category/description/entity_type/id/query`）。但池化时是**按 `subject_type=2`
圈的池子**（`build_pooled_template` 把它传给了 `hybrid_search`），评测却一直在宽 3–4 倍的域上跑。

**召回域和池化域不一致 ⇒ C 组数字偏乐观**；走生产路径后更明显，因为更宽的域会喂大
阶段 2 的软兜底填充池。已修：从 `POOLED_QUERIES` 回填。

> 副作用见上：C 组的下跌里有它的一份。**修完也不能说 C 组"变差了"** ——
> 是它一直虚高，现在才落到真实位置。

### 12.3 `--check` 重建：6 条不变量 → I1–I8 + 棘轮 R1–R3

旧版 `--check` 是 6 条不变量，且**只覆盖检索层**。重建后：

- **8 条硬不变量 I1–I8**：分列检索层 / 融合层 / **解析层**（I5）/ 标注层（I7）/ **测量仪器**（I8）。
  I8 是新的一类 —— 它保的不是被测对象，而是尺子本身。
- **3 条棘轮 R1–R3**：**违规集必须恰好等于冻结集**。
  新增违规 = 发现新缺陷（须修代码）；违规减少 = 基线可收紧（须 `--freeze` 收紧）。
  冻结文件 `data/rag_gt/check_baseline.json`：

  ```
  parse_domain_support      3 条   R1 的冻结集
  keyword_stage1_nonempty   6 条   R2 的冻结集
  extraction_fingerprint   34 条   R3 的冻结集（全量解析结果指纹）
  ```

- 收紧基线：`python -m eval.rag_eval --check --freeze --reason "…"`（**只能删条目，不能加**）
- 跳过探针 pass：`--check --no-probe`（省 ~35 次 embedding；I8 的探针侧信息会缺失）

**I8 是怎么来的**：通道失败原来是 `except: pass` 静默的，于是"某通道返回 0 条"有
两种无法区分的原因 —— 它真找不到（被测对象）vs 它根本没跑起来（仪器）。
2026-09-12 实测撞到：智谱 API 连接失败 → vector **静默归零**，融合数字一个字不变。

### 12.4 ⚠️ `--check` 退出码曾经永远返回不了 0（已修）

汇总失败清单时把 **7 元组的棘轮**按 **4 元组的 checks** 形状解包：

```python
failed += [c for c, _, ok, _, _ in ratchets if not ok]   # 错：ratchets 是 7 元组
```

`ValueError` 在**所有断言跑完之后**才抛 → 「8 条硬不变量 + 3 条棘轮全绿」也返回退出码 1。
**修复前，`--check` 作为 CI 门禁的返回值没有任何意义。**

- 教训：**断言全绿 ≠ 退出码 0。凡"退出码"类结论，必须打印 `$?` 才算数。**
- 修后实测：`EXIT=0`，连跑两次输出**逐字节一致**（确定性成立）。
- 失败路径复验（篡改基线：凭空加 `zzz_fake` + 删掉真缺陷 `p01`）→ `EXIT=1`，
  分别报「R2 新增缺陷（须修代码）: p01」与「R2 基线可收紧: zzz_fake」。

### 12.5 断电实测：embedding 服务整个挂掉时，`--check` 报什么

这是 I8 存在的**实测依据**。做法是给一个坏 key，让 embedding 调用一律失败。

**(a) 真实行为 —— 探活开着**（`ZHIPU_API_KEY=bad-key`）：

```
耗时 2 秒，EXIT=1
打印「环境不可用（embedding 服务）：…」
   + 「本次【未测任何不变量】—— 不要把这次当成 PASS 或 FAIL 读」
```

**探活（canary）在 2 秒内拦下，一条不变量都不跑** —— 这正是设计意图：
一个连尺子都没校准的 run，不该产出任何 PASS/FAIL。

**(b) 对照 —— 内存里绕过探活**（改的是模块属性，**未改源码**；23 秒，`EXIT=1`）：

```
  I1 关键词通道形状契约        34/34  PASS
  I2 A 组自测回环              15/15  PASS
  I3 短路契约                  34/34  PASS
  I4 B 组精确名称排第一          7/7   PASS   ← B 组靠 name 通道（纯 SQL），服务挂了也全对
  I5 A 组前提成立              15/15  PASS
  I6 无查询静默返回空          31/34  FAIL   ← p08 帅气冷酷的男性角色 / p09 知名动画导演 / p10 配乐出色的作曲家
  I7 GT 为空清单不变             1 条  PASS
  I8 仪器健康 (探活+通道失败)    45 次 FAIL   ← 11 生产 + 34 探针，逐条报 401，带 pass 名
  R1 3/3 PASS   R2 6/6 PASS   R3 34/34 PASS
  结论: FAIL（I6、I8 破掉）
  分组: A 0.2230(不变)  A' 0.1063(不变)  B 1.0000(不变)  C 0.3068 → 0.1401
  探针表 vector 列: 全 0.0000
```

**这张表要论证的三件事：**

1. **10 条断言在 embedding 完全挂掉时一字不变**（I1–I5、I7 + R1–R3）——
   包含最像质量指标的 I1/I2/I4。**分数不动，不等于系统没坏。**
2. **I6 只红 3 条、C 组只掉一半**（不是"全塌空 / C 组归零"）。
   原因：生产是**短路**的，多数查询根本不调向量通道，断电影响不到它们。
   这不是弱点 —— 它说明这套指标的敏感度**精确对应线上真实依赖**。
3. **只有 I8 说得出"为什么"**：45 次通道失败、逐条带 401 原文和 pass 名。
   没有 I8，这次服务故障会被读成"向量通道能力差"。

**复现**（driver 只有 6 行，跑完即弃；仓库里**刻意不留** `--no-canary` 开关）：

```python
import sys; sys.argv = ["rag_eval", "--check"]
import eval.rag_eval as R
R._embedding_canary = lambda: None      # ← 唯一一处"绕过"
try: R.main()
except SystemExit as e: print("退出码 =", e.code)
```

```bash
PYTHONPATH=. ZHIPU_API_KEY=bad-key .venv/bin/python /tmp/blackout_run.py
```

### 12.6 消融实测（2026-09-13，149 秒，探针 pass）—— 推翻 §11.2 的两行

`--ablate` 跑在**探针 pass** 上（理由见下），基准 = 生产参数：

| 配置 | Recall@5 | delta | Precision@5 | MRR | NDCG@5 |
|---|---|---|---|---|---|
| `baseline (生产参数)` | 0.4231 | — | 0.7059 | 0.9069 | 0.8687 |
| `-threshold` | 0.4231 | ±0.0000 | ±0.0000 | ±0.0000 | ±0.0000 |
| `+bucketing` | 0.4132 | **−0.0099** | −0.0176 | −0.0186 | −0.0188 |
| `-mmr` | 0.4231 | ±0.0000 | ±0.0000 | ±0.0000 | ±0.0000 |
| `vanilla (全关)` | 0.4231 | ±0.0000 | ±0.0000 | ±0.0000 | ±0.0000 |

分组（Recall@5）：**只有 C 组在动** —— baseline / `-threshold` / `-mmr` / `vanilla`
均 `0.3691`，`+bucketing` `0.3317`（−0.0374）。A / A′ / B 三组五档全部逐位相同。

**四条结论（均已实测验证）：**

1. **`+bucketing` 负收益的结论不变、且更强**（§11.2 的 −0.0069 → 今 −0.0099）。
   「不该在生产开分桶」成立。
2. **`-mmr` 的 −0.0029 归零 —— 不是 MMR 失效，是检索域修对了。**
   MMR 在 `hybrid_search` 里的实现是**同名去重**（`rag/retriever.py:293-305`，按
   `name_cn or name`）。旧口径下 C 组 `subject_type` 恒为 None（12.2.1）→ 域宽 3–4 倍
   → **跨媒体同名**（同一作品的动画版与漫画版）混进候选池 → 去重才有事可做。
   回填修复后域限定到 `subject_type=2`，跨媒体副本根本进不了池子。
   **实测**：34 条查询开 / 关 MMR 返回名单逐条相同（0 条不同）；把域手动放宽成
   `subject_type=None` 后，MMR 立刻在 **a19 / a20 / p05** 上触发（去掉的是
   `进击的巨人`、`彻夜之歌`、`樱花庄的宠物女孩` 的同名副本）。
   ⇒ **不要写成"MMR 没用"**，要写成"域修对之后，重复项没机会进池子"。
3. **`-threshold` 的"零效果"（§11.5 记为"未查"）现在有解释了**：
   阈值的唯一作用是丢弃 `cosine_distance > 0.65` 的候选，而 baseline 是**纯距离排序**
   （`enable_bucketing=False`）→ 丢尾部**只能让列表变短、不能改顺序**。
   结果一位没变 ⇒ **每次召回的前 10 名本来就全部落在 0.65 以内**，阈值从未成为限制。
   （这是逻辑推论 + 实测一致，不需要再测。）
4. **`vanilla (全关)` 与 baseline 逐位相同是【必然】**：三个开关里 threshold 和 mmr
   本来就无作用，而 bucketing 在 baseline 里本来就是关的。
   **这一行看着像表坏了，README 里必须写明**，否则读者会以为消融没生效。

**为什么消融必须跑在探针 pass 上**：生产 pass 里 **23/34 条被 keyword 短路**，
向量通道根本没跑 —— 在那里做消融，整张表会几乎全平。那不是"组件没用"，是"组件没跑"。
**代价**：这张表的 baseline 行 =「向量通道单独跑」，**不等于线上表现**。

**复现**：`.venv/bin/python -m eval.rag_eval --ablate`（149 秒）

### 12.7 §11.5 那四条的现状

| §11.5 的条目 | 现状 |
|---|---|
| A 组同源问题无法在报告层消除 | 已固化为 **I2**（跌了 = keyword 通道坏了）；另有 **A′ 组**把解析缺口独立出去 |
| B 组 `Precision@5 = 0.2` 是假象 | **仍然成立**（指标不适用于 known-item 查询） |
| C 组池化偏差 | **仍然成立**（已决定不重标） |
| `enable_threshold` 零效果"未查" | ✅ **已解释**，见 12.6 结论 3 |

---

## 13. D 组：倒着出题（2026-09-13 建）

### 13.1 为什么 A–C 组之外还要再建一套 GT

A–C 组的共同点是：**答案由出题人先写死，再问检索器找不找得到**。这带来两个无法在
报告层消除的问题（§5、§10）：`|GT| ≫ K` 时 `Recall@K` 打不满，测的是 GT 定义；
`|GT| = 1` 时 `Precision@K = 1/K` 是假象（B 组）。

D 组换了方向：**从库内实体反编一条查询**。答案不再"写"，而是"被反推出来" ——
出题时以某一个 `RagEntity` 为答案，让 LLM 顺着它的简介编一句用户会问的话。
于是**每条查询的答案唯一且已知**，`Recall@K` 与 `Hit@K` 等价、`NDCG@K` 有意义、
`Precision@K` 退出。这是本轴第一次有资格算标准 IR 指标。

**GT 现状**：`data/rag_gt/ground_truth_v2_draft.json`，**450 条**，分组键是
`entity_type · style`。**各组条数不齐**：subject 的 plot/tag/title 各 **120** 条，
character 与 person 的六组各 **15** 条（3×120 + 6×15 = 450）。
状态 `draft_unfiltered` —— **未冻结**。

### 13.2 两层分工

| 层 | 做什么 | 产出 | 花钱吗 |
|---|---|---|---|
| **层 A** | 生产检索跑一遍（同 §12 的口径，`limit=20`） | `Recall@5/10/20 · NDCG@20 · MRR` | 只花 embedding |
| **层 B** | LLM 判官盲判候选池每一条 | **诊断**（不是指标）+ **κ 校准** | 判官调用，有缓存 |

候选池三部分互斥：`top-20`（生产返回的）、`随机 15`（域内随机，**排除 top-20 与出题
实体**）、`探针`（出题实体本身，用于层 A×层 B 互验）。

**判定缓存** `data/rag_gt/judge_cache.jsonl`：键是 `sha1(schema + 查询 + 候选卡片)`，
`schema` 里含 **rubric 指纹**。**改 rubric = 全部旧判定失效**（会重新判，不会静默复用）。
`think=0` 是 schema 里的**冻结字面量**，别去"清理"它 —— 清了会让 A/B 期 395 条
不可再生的判定全部作废（§13.5）。

### 13.3 判官校准：κ = 0.831

50 条人工标注（`data/rag_gt/annotation_sheet_b.md`，答案纸 `annotation_key_b.json`）。
样本按判官判定分层抽（true 20 / false 20 / 名字咬得上却判 false 的探针 10），**抽完洗牌**
（`_sample_for_annotation` 末尾的 `rng.shuffle`）—— 分层是为了让两类都有足够样本，
洗牌是为了让"上下两条"没有顺序含义。判官判定**不写进表里**，只在 key 里，
所以标的时候看不到判官怎么判的。

⚠ 这份表是**历史产物，不要重建** —— 标完的 50 条是已发生的事实记录。表头里那两条
示范（「同一系列的另一季 → true」）是**旧口径**的写法，见下面的保守下界说明。

| | 值 |
|---|---|
| n | 50（全部进了 κ） |
| po | 0.940 |
| pe | 0.644 |
| **κ** | **0.831**（Landis & Koch: `almost perfect`） |

**按查询风格分层**（比 κ 本身有用）：

| 风格 | n | po | κ |
|---|---|---|---|
| title —— 查询点名了具体作品 | 18 | 1.000 | **1.000** |
| plot —— 描述剧情 | 17 | 0.941 | 0.767 |
| tag —— 描述品类 | 14 | 0.857 | 0.696 |

**分歧全部落在「tag/plot 式查询 × 同系列候选」这一格**（3 条：`#7` / `#23` / `#30`）。
50 条里只有 **12 条**候选与出题实体同系列/IP —— 口径差异只在这 12 条上起作用。

⚠ **2026-09-13 晚复核：早先的读法是错的，已推翻。** 早先写「那类查询问的是**品类**，
本来就没有唯一答案 → 是单答案 GT 的适用边界，**不是判官判错**」。把 12 条摊开看，
真实情况是**两条方向相反的问题**：

**① 3 条分歧里，判官全对、人工全错（κ 因此是保守下界）**

| 行 | 查询 | 候选 ← 出题 | 判官 | 人工 | 按 b4 口径 |
|---|---|---|---|---|---|
| `#7` | 京阿尼校园恋爱搞笑动画 | 中二病也要谈恋爱！Lite ← 中二病也要谈恋爱！ | false | true | **判官对** |
| `#23` | 最弱猎人临死前…独自变强 | 我独自升级 第二季 -起于暗影- ← 我独自升级 | false | true | **判官对** |
| `#30` | SUNRISE的校园偶像原创动画 | Love Live! 第二季 ← Love Live! Superstar!! | false | true | **判官对** |

原因不是"GT 不适用"，是**标注表头与 b4 不是同一版**：表头（`b-anno-1`，rubric
`6cc0e9d5`）写着「同一系列的另一季 → true」，人工是照那句判的；判官照 b4 判 false。
另 9 条同系列行（空之境界其他章 ×4、JOJO 其他部 ×2、中二病Lite/我独自升级二季/
LoveLive二季）判官与人工**一致判 false**。

**② 另有 2 条判官与人工【一起】违反 b4 —— κ 把它们白记成"一致"**

| 行 | 查询 | 候选 ← 出题 | 判官 | 人工 | 按 b4 口径 |
|---|---|---|---|---|---|
| `#24` | SUNRISE的校园偶像原创动画 | Love Live! 虹咲学园 ← Love Live! Superstar!! | **true** | true | ❌ **判官错** |
| `#45` | 京阿尼校园恋爱搞笑动画 | 中二病也要谈恋爱！恋 ← 中二病也要谈恋爱！ | **true** | true | ❌ **判官错** |

b4 明写「同一系列或同一 IP，但不是查询点名的那一部 → 全部判 false」，这两条判 `true`
是判官的**真实误判**，而人工照旧表头也判 `true` —— **κ 只测一致性，测不出"双方一起错"**。
⇒ 判官在「同系列」这条规则上**自己不稳定**：中二病 **Lite 判 `false`**，中二病 **恋判
`true`**，同一条规则两种结果。**影响面仅限层 B 的「同系列拆分」诊断表 —— 判官不参与
层 A，所以层 A 的 IR 指标不受影响。**

**③ 因此 κ=0.831 要连着两个方向一起报**，否则会被读成"判官 83% 对"：

- 它是**保守下界**（①，判官被扣的分不是它的错）；
- 它同时**高估**判官（②，κ 抓不到共同错误）。

**净效果**：若把这 12 条按 b4 全部重标，`po` 0.940 → 0.960、`pe` 0.644 → 0.704、
**κ 0.831 → 0.865** —— 3 条分歧消掉（+），2 条假一致暴露成真分歧（−）。**不是抬到 1.000。**
（且此复标已不再盲，要干净只能整批 50 条重出表重标。）

⚠ **还有一点必须报**：
1. **分层是看到结果之后才做的**，不是预注册结论。
2. 报告会自动比对 rubric 指纹，在不一致时打印保守下界警告（`_kappa_md`）。

### 13.4 「相关率」退役 —— 单答案 GT 不许报 Precision@K

层 B 原本的主指标是 `top-20 相关率`（= Precision@20）配 `相对全域随机的增益`。
**2026-09-13 修正：这条路在单答案 GT 下是死的**，两条独立的原因：

1. **Precision@K 退化成命中率的复述**：相关 = 「就是点名的那一部」，一条查询只有一个
   答案，于是 `Precision@20 ∈ {0, 0.05}`，`Recall@20 = Hit@20`。
   实证（`_compute_metrics`，目标排第 3）：`Recall@20 = 1.0` 而 `Precision@20 = 0.05`。
2. **随机基线结构性为 0**：随机补池按构造**排除了出题实体**，而相关又只认那一条
   → 池里不可能有相关条目。实测 b4 口径下**全组 0.000**、漏检证据全组 **0**。
   **这不是测量结果，是构造决定的**，印出来只会被当成测量结果。

**处置**：层 B 主表与「漏检证据 / 粗估召回率」两表已从报告删除，原始值保留在 JSON
（`table[*].gain` / `table[*].estimated_recall`）里作记录。层 B 只留三张诊断表：
**同系列拆分 / AND 落空 / 探针互验**。轴 2 的质量数字归层 A。

**指标口径**：`eval/metrics.py` 的标准实现，单答案 GT 下读作 ——
`Recall@K ≡ HitRate@K`（命中率），`NDCG@K = 1/log2(rank+1)`（罚排位，是 Recall 的加细），
`MRR = 1/rank`。**别把它们读成有序性 / 排序质量**：GT 只有一条，NDCG 与 MRR 是同一个
位置的两种折扣。

### 13.5 同一个坑踩了两次

`Precision@5 = 0.2`（B 组，§11.5 / §12.7）与 D 组的 `相关率` 是**同一个错误**：
`|GT| = 1` 而 `K > 1` 时报 Precision。第一次记在 README 里了，第二次还是踩了 ——
因为 D 组换了名字（「相关率」而不是「Precision@K」），绕过了那条已经写死的结论。

**教训写进规则**：**报任何指标前先问"它的分母是不是 K，而分子是不是只有 1 条"。**
`eval/README.md` 的轴 2 节与 `generate_rag_gt.py` 的模块 docstring 都留了这条。

### 13.6 顺带修掉的两个 bug（都影响可信度）

1. **`_ANSWER_RE` 漏 `re.M`** —— `^` 只认整串开头，`_count_answered()` **恒返回 0**，
   于是「标注表已填、别覆盖」的防呆是**死的**：下次跑 `--annotate` 会静默覆盖已填的
   50 条人工标注（不可恢复）。修完返回 49（#14 的答案写在上一行，49 是对的）。
2. **κ 的双重舍入** —— `cohen_kappa` 先 `round(...,4)`，报告再 `:.3f`，把 0.83146
   印成 **0.832**；直接格式化原始值是 **0.831**。同一份数据两个数。
   已改为只保留 6 位，舍入留给展示层。

### 13.7 现状与待办

| 项 | 状态 |
|---|---|
| 层 A 全量 450 条 | ✅ **已跑**（`rag_gt_layer_a-20260913-234241-7268059.md`，含 NDCG@20 列）。早先那份旧格式报告（203148）已归档 |
| 层 B 试点 21 条 | ✅ 已跑（判定全命中缓存、0 次调用） |
| 判官 κ | ✅ 0.831（保守下界） |
| GT 冻结 | ⬜ 仍是 `draft_unfiltered` |
| 生产融合短路缺陷 | ⬜ 未修（`tools/bgm_tools.py:1091/1108`）—— 修完用**同一份 GT** 重测 |

**复现**：
```bash
.venv/bin/python -m eval.generate_rag_gt --evaluate            # 层 A（只花 embedding）
.venv/bin/python -m eval.generate_rag_gt --judge --sample 20   # 层 B 试点（判定全命中缓存 → 0 次调用）
.venv/bin/python -m eval.generate_rag_gt --annotate            # 出标注表（已标过，防呆会拦）
```

---

## 14. 工具返回超出记忆层容量（2026-09-14 修）

这一节记的是**生产缺陷**，但它连带改掉了轴 3 的证据口径，所以 01:42 那份 n=30 冻样本
**作废**（§14.5）。

### 14.1 怎么发现的

轴 3 Tier 2 的步骤 0（判官侧证据投影 dry-run）报出**单场景 6.97 万字 / 62 张卡片**。
追下去发现问题不在 eval，在 `search_local_bangumi` 的返回体。

### 14.2 实测（用冻结样本里的 id 回查 `rag_entities` 逐条重建真实 payload）

| | 值 |
|---|---|
| 11 次 `search_local_bangumi` 调用 | **11/11 全部超限** |
| 真实 payload token | **中位 20,522**，最小 13,549，最大 41,677 |
| L1 单条消息上限（`agent/memory/short_term.py`） | **2,000** |
| 超出倍数 | **6.8× – 20.8×** |
| 完整进入模型的记录 | 114 条里 **5 条（4.4%）** |

`manage_memory` 的 `_truncate_oversized_messages` 掐到 2000 token（`_truncate_text_by_tokens`
保留 `tokens[:available]`，**弃尾保头**）—— 于是从 JSON 中间切断，留下的恰好是 keyword 通道
排名最高的那 1–2 条。**模型以为自己在一个 10–15 条的候选列表里挑，实际只看得见前 1–2 条。**

> **测量订正**：先前报的「中位 9,379 token」是**录制侧 `card-v1` 投影之后**的尺寸（冻样本的
> `evidence` 带 `carded: True`），**不是工具原始返回**。真值 20,522。结论方向不变，
> 严重度是 2.2 倍。**这个订正本身就是 `carded` 标记存在的理由**：带投影标记的数字是下界。

### 14.3 修法（产出侧，不在记忆层）

`search_local_bangumi` 不再把每条 `rag_output` 原样拼出去，改为投影成**选择用索引卡**：

- 保留 `id / name / name_cn / type / date / eps / score / rank / info / tags / entity_type`
  （character 另带 `role`、person 另带 `career`），**丢掉 `summary` / `infobox` / `collection` /
  `rating_count` / `rating_total`**。docstring 明写「要剧情简介、制作班底，拿 id 去调 detail 工具」。
- 整块字符预算 **2200**，**超了整条丢、绝不切单条**；`total > 0` 时**保底放第一条**
  （否则降级话术会把「找到了 10 条」说成「没找着」）。
- 返回信封 **在前**：`{"total": 检索到的条数, "shown": 实际返回的条数, "results": [...], "note": ...}`
  —— `note` 仅在 `shown < total` 时出现。**语义变更**：`total` 从「返回条数」变成「检索到的条数」
  （其余带 `total` 的工具仍是 `len(results)`）。
- 连带修两处：记忆层压缩把 `search_local_bangumi` 接进已有的 `_compress_search_result`
  （否则下一轮又把刚投好的索引卡掐成 ~299 token 的坏 JSON）；压缩行补 `role`/`career`
  与**扁平** `score`/`rank`（原来读的是嵌套 `item["rating"]["score"]`，真实形状是扁平的 →
  评分排名一直被静默丢掉）。
- 契约测试：对整个 `json.dumps(payload)` 数 token ≤ `_MAX_SINGLE_MESSAGE_TOKENS`、
  `id` 存活、`shown < total` 时 `note` 在、病态输入走保底分支、`shown` 对 `limit` 单调
  （`test/test_bgm_tools.py`）。

### 14.4 顺带查清、本轮**不修**的

1. **库里 `rag_output` 是陈旧的**：1030 条 subject 里 **1005 条（97.6%）** 的 infobox 含非白名单键
   —— 语料灌在 P5 修复（`b513cbc`，28 键白名单）**之前**，之后没重灌。字段实测均长：
   subject `tags` 932 字、`infobox` 783 字（最大 7,158）、person `summary` 1,027 字。
   **即使重灌也解决不了本缺陷** —— 10 条记录哪怕全过 sanitizer，光 `tags` 一项就注定超 2000。
   **问题在出口不在入库。**
2. **超限不止一个工具**：`search_bangumi_subject` **8/12 超限**（中位 2,265）、
   `get_subject_opinions` **4/5 超限**（中位 2,129），其余 7 个工具 0 超限。**未修**。
3. **`type` 键的语义分歧**：API 路径给「动画」，RAG 路径给「TV」。本轮加 `entity_type` 绕开，
   分歧本身保留。

### 14.5 对 eval 的影响：轴 3 证据口径 `card-v1` → `index-v1`

工具形状变了而 policy 字符串不变，新旧样本会在缓存/对比口径上被当成同一种证据 ——
所以 `_EVIDENCE_POLICY` 一并 bump，同时把 `search_local_bangumi` 从 `_CARDED_TOOLS` 移出
（工具自己已经有界，录制侧再投一次影是纯冗余）。

| | 01:42 样本（作废） | 15:23 样本（当前） |
|---|---|---|
| 证据口径 | `card-v1`（录制侧还投影一层） | `index-v1`（**录制侧不投影**，盘上存工具原样返回） |
| 卡片化条数 | 11 | **0** |
| L1 截断 / 失败调用 | 12 / 4 | 12 / 4 |
| 工具调用 | 48 | 46 |

**两份样本的证据不可比** —— Tier 1 的「超软上限 20.0% → 6.7%」方向与修复一致，
但两次录制之间还夹着压缩改按形状分派、兜底截断修复等改动，**没有做单变量对照，不要写成因果**。

---

## 15. 轴 3 Tier 2：忠实性判官（2026-09-14 建）

### 15.1 判据：为什么「证据里没有」判 unsupported 而不是 unknown

判的是「回复里的**事实断言**能不能在 Agent **当时看到的东西**里找到依据」。

关键在于**判官视角 ≡ 模型视角**：证据块按 `json.dumps(..., ensure_ascii=False)` 序列化
（与 LangChain `_stringify` 同路，**46/46 byte 对账一致**），超 2000 token 的走生产同款
`_truncate_message_content`（弃尾保头）。而 `manage_memory` 在**每一轮 LLM 调用前**都跑 ——
所以**模型从未见过完整版**。「证据里没有」直接等于「模型当时没有」，判 unsupported。

证据块头的「完整 / 已截断 / 失败」（12 / 4）是**证据完整度的读数**，不用来产生 unknown。
`unknown` 的门槛很高：仅证据整块缺失或断言无从核对。实测 **unknown = 0**。

### 15.2 出数（n=30 场景 / 138 断言）

样本 `results/frozen/reply_quality-20260914-152308-db2f3c8.json`
报告 `results/reply_quality-tier2-20260914-165422-db2f3c8.md`
判官口径 `faithfulness-v1|cap=2000|18356f9d` ｜ 模型 `deepseek-v4-flash` 温度 0.0

| | 值 |
|---|---|
| **断言级 unsupported 率**（主） | **34.8%**（48 / 138） |
| 摘掉工具自述（口径 B） | **33.6%**（45 / 134） |
| 有 unsupported 断言的场景 | 66.7%（16 / 24） |
| supported / unsupported / unknown | 90 / 48 / 0 |
| 判官调用 / 缓存命中 | 0 / **54**（判定侧可反复跑，0 成本） |

**分母口径**：30 条场景 = 可判 **24** + 零证据 **6**（`A1/A2/B5/B6/C1/E5`，一次工具都没调）。
零证据场景**不进任何分母** —— 混进去会把「没调工具」和「有据不用」搅成一个数。

**这个数不是幻觉率**：unsupported 只说明「这句在工具返回里找不到依据」，实测分三类 ——
① 模型自带的知识（多半是对的）、② 对作品的解读（工具返回里本来就没有这类东西，
★ 2026-09-14 拍板照判 unsupported）、③ 真幻觉。要分三类只能靠人工读。

### 15.3 ★ 缓存键缺陷：一次「κ 涨了但涨的是错位」的事故（我的错）

**这是本文档里唯一一处由我引入、并一度污染过结论的缺陷，全记在这里。**

第一版 `_rubric_fingerprint()` 是 `sha1(_SPLIT_RUBRIC + _CHECK_RUBRIC)`，而 `_cache_key()`
**两段共用这一个指纹**（`git show 09ada3d:eval/reply_quality_judge.py:284-301`）。
于是改**核对**口径把**切句**缓存一起作废了，30 个场景全部重新切句 ——
而 LLM 切句不是逐字可复现的：

| 场景 | 人勾的那条断言 | 重判后同一序号上变成 |
|---|---|---|
| `B1` | 《轻音少女》是**温柔向的作品** | 《轻音少女》**是作品** |
| `D5` | **辛美尔那条线是公认的情感核心** | 《芙莉莲》的**标签里有治愈** |
| `D1` | 趋势榜前三大概是 Re:Zero、尼古喵喵、无职转生 | 趋势榜前三**包括 Re:Zero 第四季** |

人工标注按 **`(场景, 序号)`** 存档，序号一漂，人勾的答案就对到别的断言上去了 ——
**39 条里 19 条错位**，κ 一度算出 **0.748**（错位的功劳，不是修得好）。

> ⚠️ **这一段是当时实测的记录，中间那三份报告（164859 / 165149 / 165304）已经被删**，
> 现在无法从产物复现 19 这个数。可复现的部分：① 缺陷代码在 `09ada3d` 里；
> ② 修复（两个指纹解耦 + `_cache_key` 按 stage 取指纹）在工作区；
> ③ 缓存里有 **30 条 `migrated_from` 标记**（`grep -c migrated_from eval/data/reply_quality_judge_cache.jsonl`）；
> ④ 修复后 39/39 条断言文本与答案纸逐字一致（比对最终报告与 `annotation_key_tier2.json`
> 的 `text` 字段，用一次性脚本做过，**没有固化成命令**）。

**修法**：`_rubric_fingerprint()` 只含 `_CHECK_RUBRIC`，新增 `_split_fingerprint()` 只含
`_SPLIT_RUBRIC`，`_cache_key(stage, ...)` 按 stage 取。另做**一次性重键**：把 30 条切句记录
从旧键搬到新键（append-only 追加，带 `migrated_from`），让这批切句在新键下原样命中 ——
**断言身份不漂，人不用重勾**，这才是真前后对比。

> **顺带一条别踩**：`annotation_key_tier2.json` 里的 `judge_prev` 只在「判定变了」时写，
> **多次刷新会覆盖**。所以它**不能**用来复原历史 κ —— 用它复原出来是 0.471，
> 既不是旧口径的真值也不是任何一次真实运行的结果。要旧口径的 κ 只能回到
> `153919` 那份报告本身算（见 §15.4）。

### 15.4 口径修正的效果：38.4% → 34.8%，κ 0.683 → 0.889

| | 旧 rubric（`153919`，指纹 `ef59def6`） | 新 rubric（`165422`，指纹 `18356f9d`） |
|---|---|---|
| unsupported 率 | 53/138 = **38.4%** | 48/138 = **34.8%** |
| κ（n=39） | 0.682927 → **0.683** `substantial` | 0.888889 → **0.889** `almost perfect` |
| po / pe | 0.846 / 0.515 | 0.949 / 0.538 |
| 判官=supported，人同意 | 21/21 = 100% | 24/24 = 100% |
| 判官=unsupported，人同意 | 12/18 = 67% | 13/15 = 87% |
| 按 depth 分层 | — | deep 6 条 κ=1.000 ／ fast 33 条 κ=0.870 |

> **新 rubric 那一列的 κ 分两步落定，两个读数都留着。**
> 刚重判完是 **0.831169**（po 0.9231 / pe 0.5444，`unsupported` 侧 12/15，残余分歧 3 条）——
> 那是标注表**原始勾选**的读数。随后发现 `#16`（`D3`「趋势榜反映的是当前热度，不是评分涨幅」，
> 属**工具自述**）在表里勾的是 `supported`，与预注册口径矛盾：规则 5 要求这类断言照常判 `v`
> （证据里没有 ⇒ `unsupported`）、只额外打 `tool_self` 标记，再由**口径 B** 把它整个摘出分子分母。
> 2026-09-14 按口径把这一格校正为 `unsupported` ⇒ **0.831169 → 0.888889**：
> `unsupported` 侧 12/15 → 13/15、残余分歧 3 → 2 条、fast 分层 0.802 → 0.870。
> **这是口径校正、不是重新判定** —— 判官一个字没改（该条 `judge` 自始至终是 `unsupported`，
> 见 `annotation_key_tier2.json` 的 `#16`），动的是人工那一格。校正同时消掉了它与
> 轴 2 D 组 κ（0.831，n=50）的数值撞车。
>
> **两个读数都可复现**（标注表以**校正后**的状态入仓）：现行表跑 `--tier2-kappa`
> ⇒ **0.888889**；把 `#16` 那一格改回 `supported` 再跑 ⇒ **0.831169**（校正前读数）。

改判 **10 条：8 条变对、2 条变错**。**这不是单向放宽** —— 其中 **4 条是判官反过来变严**
（人本来就判 unsupported，旧判官错判成 supported）：`E3` 「甜而不腻的日常向」、
`E2` 「《巨人之星》是一部名为「巨人」的作品」、`D1` 「尼古喵喵最近在播」、
`D4` 「《莉兹与青鸟》是山田尚子执导的」。

**复现**（离线、免费）：
```bash
# 新口径的 κ（读标注表 + 答案纸，同时打印混淆矩阵与分层警告）
.venv/bin/python -m eval.reply_quality_eval --tier2-kappa

# 旧口径的 κ 没有现成命令 —— 只能拿 153919 报告与答案纸现配一次。
# key 里的 judge_prev 不可用（会被多次刷新覆盖，见 §15.3）。
```

### 15.5 出处核对：两把尺子

supported 的断言，判官会给出处；出处拿回**证据**里核两遍：**逐字核**（允许用省略号拼两段）
与**宽松核**（拆成片段，每片都要在证据里出现，不看顺序）。

| | 条数 |
|---|---|
| 逐字核不到 | **8** |
| 宽松核不到 | **0** |
| supported 无出处被降级 | **0**（应恒为 0） |

**逐字那一列高是正常的，不是错** —— 「找到依据」放宽之后，判官引的是**依据**而不是原句
（`rating_total: 36196` 这种字段名+值的改写、重排过的标签表）。要看的是宽松那一列：
它才是「判官报了个证据里根本没有的数字/名字」。伪造照样拦得住：编的 `13话` 而证据写 `12话`
→ 两把尺子都不过（已用 7 个合成用例验过）。

### 15.6 分层样本与残余分歧

⚠️ **κ 是分层样本上的，不是总体加权。** 抽样按**判官判定**分层，分层冻结于抽取那一刻 ——
总体 supported:unsupported = **85:53**，抽样分层 **21:18**，重判之后实际构成 **24:15**。
**三个比例要连着报**，否则 κ 会被当成总体值读。

**残余分歧 2 条，方向一致（判官严于人工）**；24 条判 supported 的人**全同意**：

| 场景 | 断言 | 判官为什么不认 |
|---|---|---|
| `D6` | `deepseek_jiang` 这个 ID 的收藏、日志、时间线都是空的 | 证据是 `collections_error: 未找到资源`；判官要「未说明为空」，人认为 404 就是没有 |
| `D5` | 辛美尔那条线是公认的情感核心 | `公认` 在证据里出现 **0 次** |

（原先还有第三处 `D3`「趋势榜反映的是当前热度，不是评分涨幅」—— 工具自述，已按预注册口径
校正为 `unsupported`、与判官一致，见 §15.4 的方框。）

**工具自述单列**：模型陈述「工具本身是什么 / 我手上有什么」的断言（共 **4** 条，
其中 3 条判 unsupported，全在 `D3`）核对对象是 Agent 自己的工具集，不是作品事实 ——
**口径 B 把它整个摘出分子分母**。

### 15.7 现状与待办

| 项 | 状态 |
|---|---|
| Tier 2 判官 | ✅ 已建、已出数（n=30 / 138 断言） |
| 人工 κ 校准 | ✅ 0.889（n=39，分层样本；`#16` 口径校正后，见 §15.4） |
| 判官 ≠ 被评模型（工程约束 3） | ⬜ **做不到** —— 判官就是 `deepseek-v4-flash`，只能声明 |
| 离线测试（mock LLM） | ⬜ 未建 |
| `search_bangumi_subject` / `get_subject_opinions` 超限 | ⬜ 未修（§14.4） |
| 巡检脚本（逐工具报 token vs 上限） | ⬜ 未建 |
